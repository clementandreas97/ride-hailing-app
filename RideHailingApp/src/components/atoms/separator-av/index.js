import React from 'react';
import {View} from 'react-native';

const SeparatorAV = (props) => {
  return (
    <View style={{height: props.customHeight, backgroundColor: '#F2F2F4', marginTop: props.customMarginTop}}></View>
  )
}

export default SeparatorAV;